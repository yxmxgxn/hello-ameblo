# 引き継ぎメモ（2026-09-24 時点）

このファイルは、**別の会話でこのサイトの運用を引き継ぐための資料**です。
README.md が「何がどこにあるか」なのに対して、こちらは「いまどうなっていて、何を待っていて、なぜそうしたか」を書きます。
作業のたびに、末尾の「作業ログ」と「いまの状態」を更新してください。

---

## 0. 最初に読む3行

- 公開URL: https://hello-ameblo.yxmxgxn.workers.dev （**noindex。お披露目はまだ**）
- リポジトリ: `C:\Users\mouse\ameblo-search`（GitHub: yxmxgxn/hello-ameblo、public）。**karin-archive とは別物**
- クロールは毎時7分に自動で回っている。**触らなくても動く**。壊れたときだけ直す

---

## 1. いまの状態（2026-09-24 未明）

| 項目 | 値 |
|---|---|
| 取り込み済み | 約 198,000 / 348,947 記事（57%） |
| ブログ数 | 73（遡り完了 52） |
| D1 サイズ | 約 823MB → 全部入って約1.7GB 見込み |
| よみがな | 161人ぶん取り込み済み（あいうえお順が動く） |
| 書き手が決まっていない記事 | 26件前後（後述の修正が回れば） |
| 最新コミット | `1b22896` |

初回の遡りが終わるまで、あと2〜3日。終われば `blogs.done` が全部1になる。

---

## 2. 設計の決めごと（変えるときは理由ごと考える）

ここは**過去に議論して決めたこと**。安易に反対のことをしないでほしい。

1. **アメブロに迷惑をかけない。** 1リクエスト1秒、並列なし、1回の実行で最大3000リクエスト・50分。
   全ブログが同じ ameblo.jp なので、並列化しても負担をかける先は同じ。速くしない。
2. **記事一覧（entrylist）で済むことは記事ページを開かない。** 一覧1ページで20件分の
   タイトル・テーマ・4つの日時・公開状態が取れる。編集の判定も一覧だけでやる。
3. **消すのは「もう無い」と確かめたときだけ。** 404/410 か、`publish_flg` が open 以外。
   タイムアウトや5xxは "unknown" で消さない。**消す前に必ず Discord に通知**（取り返せないため）。
4. **推測で書き手を埋めない。** 誰の記事か決められないものは空欄（`member_no IS NULL`）。
   タイトルから機械的に判断するのは、**個別に確認した例外だけ**（`crawler/fixes.tsv`）。
   ユーザーの明言: 「タイトルから分類はあくまでもこれらの例外処理を一つずつしただけで、ほかは絶対にしないこと」
5. **黙って切らない・黙って無視しない。** 検索語が長い/語が多い/記号だけ、は理由を画面に出す。
   長すぎる検索語は URL にも履歴にも残さない。
6. **本文は全文表示しない。** 一致箇所の前後40字だけ出して ameblo.jp へ送る。画像・動画は1バイトも保存しない。
7. **正規化と bigram 化は Worker 側に1か所だけ。** 索引を作る関数と検索語を処理する関数が同じでないとズレる。
8. **ひらがなとカタカナは別扱いのまま**（ユーザー判断「厳密に別で行こう」）。
9. **ソースコードは公開しない**（同じクローラが増えるとアメブロの負担になるため）。
   そのかわり `/tech` に実装を全部書いて透明性を担保する。
10. **デプロイは毎回ユーザーに確認してから。**

---

## 3. 動いているもの

### 自動で回るもの
- **毎時7分**: Cloudflare Cron Trigger → Worker の `scheduled()` → GitHub API の `workflow_dispatch` → `crawl.yml`。
  GitHub の `schedule` は混雑時に飛ばされたので使わない。
- **クロール1回**: 50分または3000リクエストで自分から止まる。`concurrency: crawl` で同時実行は1本。
- **3時間止まったら** Discord に通知（`checkStalled`）。
- **削除チェックが全ブログを一周するたび**（5日ほど＝ほぼ週1）、書き手が決まっていない記事を Discord に通知（`reportUnassigned`）。
  周回数は `checks.cycles` の最小値、最後に知らせた周回は `state` テーブルの `unassigned_notified`。

### 手で触るもの
- **SNSスプシ（accounts タブ）**: どのブログ・テーマが誰か。`ameblo`（ブログ丸ごと）と `ameblo_g`（ブログ＋テーマ番号）。
  **直せば次のクロールで既存の記事の紐づけも直る**（`crawlTargets` が差分を見て付け直す）。
- **メンバースプシ（gid=837040930）の「よみがな」列**: あいうえお順に使う。
- **`crawler/fixes.tsv`**: スプシでは表せない例外。書式は4つ。
  ```
  theme  <ブログ>/<テーマ番号>        <番号 or ->   テーマ単位で割り当て／取り込まない
  entry  <記事番号>                   <番号 or ->   記事1本ずつ
  title  <ブログ>/<テーマ番号>  <語>  <番号>        そのテーマの中で、タイトルの署名で決める
  default <ブログ>/<テーマ番号>       -             そのテーマで署名が無い記事は空欄にする
  ```
  `-` は「取り込まない（取り込み済みなら消す）」。`title`/`default` は**必ずテーマを指定する**
  （指定しないと、テーマがちゃんと付いている記事まで書き換えてしまう。実際に一度やらかした）。
- **`crawler/exclude.txt`**: 巡回しないブログ。削除依頼が来たらここ。

---

## 4. よくある作業のやり方

### 書き手が間違っている／空欄になっている
1. まず**テーマ全体が1人に対応するか**を見る。対応するなら **SNSスプシを直す**（例外表に書かない）。
   ユーザーの方針: 「できるだけ例外は少なくしたい」
2. テーマ無し・スタッフ投稿など、スプシで表せないものだけ `crawler/fixes.tsv` に1行ずつ足す。
3. push すれば次のクロールで自動的に反映される（`/api/crawl/fix` が毎回流れる。何度流しても同じ結果）。

### いまどうなっているか調べる（読むだけ。書き込みはしない）
```bash
cd /c/Users/mouse/ameblo-search
npx --yes wrangler@4 d1 execute hello-ameblo --remote --json --command "SELECT count(*) v FROM entries"
```
※ Windows のコンソールは cp932 なので、日本語を含む結果は `python -c "print(...)"` で直接出すと
UnicodeEncodeError になる。**いったんファイルに書いてから `cat` する**のが確実。

### 手元で動かす（本番D1を汚さない）
```bash
# 空のDBを作って、そこに対して dev サーバーを立てる
npx --yes wrangler@4 d1 execute hello-ameblo --local --persist-to <作業用フォルダ> --file schema.sql
npx --yes wrangler@4 dev --port 8788 --persist-to <作業用フォルダ> --var CRAWL_TOKEN:dev
```
**`wrangler dev --remote` は本番D1に繋がる**（読むだけなら可。クローラを向けると本番の対応表を上書きするので厳禁）。

### デプロイ
`main` に push → `deploy.yml` がスキーマの列追加（`continue-on-error`）とデプロイを実行。
`crawler/**` の変更はデプロイ不要（`crawl.yml` が毎回 main をチェックアウトする）。
**push の前に必ずユーザーに確認する。**

---

## 5. 待っていること・宿題

| # | 内容 | 状態 |
|---|---|---|
| 2 | 初回の遡り完了 | あと2〜3日。完了後に `/about` の件数が自動で変わる |
| 3 | RSS配信 | ユーザーの思いつき。「好きなメンバーだけのRSS」。優先度低。`/api/search` の絞り込みをそのまま使える |
| 4 | tech記事の外部転記 | Qiita に仕組み、note に説明書を転記してSEO、という構想。まだ |
| 5 | お披露目 | 「完全究極体になってから」。いまは全ページ noindex |
| 6 | GH_TOKEN の期限 | **2027-09-20 に失効**。切れるとクロールが起動しなくなる |

---

## 6. 過去に踏んだ地雷（同じことをしないため）

1. **変更の片側だけ入れた** — Worker の `/api/crawl/range` の返り値を `ids` から `rows` に変えたのに
   クローラを直さず、`KeyError: 'ids'` で2回ぶんのクロールが落ちた。**Worker とクローラは必ず同時に直す**。
2. **正規化の文字範囲を広げすぎた** — U+FAFF まで入れたらサロゲートペアを踏み、絵文字より後ろの
   スニペット位置が全部ずれた。`\u3400-\u4dbf\u4e00-\u9fff` までに留める。
3. **付け直しの条件を絞りすぎた** — 「すでに4人の誰かになっている記事は調べ直さない」としたため、
   対応表でブログ丸ごと田村芽実になっていた3,752件が直らなかった。
4. **タイトル指定をブログ単位にした** — テーマがちゃんと付いている838件の書き手を空欄で上書きした。
   `title`/`default` は必ず `<ブログ>/<テーマ番号>`。
5. **GitHub の schedule は当てにならない** — ピーク時に飛ばされる。Cron Trigger から叩く。
6. **D1 無料枠** — 500MB と1日の書き込み行数で止まった。Workers Paid（月$5、アカウント単位）で解決済み。
7. **Google スプレッドシートの公開CSVは5分キャッシュ** — 直後に取りに行っても古い内容が返る。

---

## 7. 画面まわりで決めたこと（UIの意図）

- **トップは検索なしで新着一覧**。当日の記事に `NEW`。続きは「もっと見る」**だけ**
  （自動スクロールは一度入れたが「一度オンにしたら消しにくい」という理由で**廃止**）。
- **検索中に一覧を空にしない**。前の結果を薄く残し、届いたら一度に差し替える（空にすると画面が跳ねる）。
- **読み込み中の空の一覧には高さを持たせる**（`.results:empty { min-height: 70vh }`）。これが無いとページを開くたびに跳ねる。
- **検索欄を空にしたら、その場で検索前の一覧に戻る**（Enter 不要）。
- **メンバー選択**: グループのタグは「一覧をそのグループに絞る」切り替え。押しても誰も選ばれない。
  まとめて選ぶのは「全員選ぶ」ボタン。選んだ人はグループを切り替えても残る。
- **スマホでの使いやすさを重視**（ユーザー明言）。パネル上部は狭い画面では入力欄が1行を使う。

---

## 8. 作業ログ（新しいものを上に）

- **2026-09-24** スマイレージ サブメンバーの残り4件を確認して割り当て（これで判断待ちの記事は無し）。
- **2026-09-24** タイトル指定をテーマ単位に変更。テーマ付き838件の書き手が消える不具合を修正し、
  「対応表があるのに空欄」の記事を埋め直す処理を追加（`1b22896`）。
- **2026-09-23** よみがな取り込み、テーマ無しの定期通知、例外表（`fixes.tsv`）の仕組みと初期データ。
  Lovendor 2,161件削除、℃-ute 鈴木愛理 870件の紐づけ修正、スマイレージ サブメンバー3,900件を4人に分配。
- **2026-09-23** `/about` 書き直し、`/tech` 新規作成、全ページ noindex、検索語の上限（100文字・5語）の明示。
- **2026-09-22** 日時4種（`published` / `ins_datetime` / `edited` / `upd_datetime`）の保存と、
  `last_edit_datetime` での編集検出。テスト記事で編集・削除を実測（削除は404が返ることを確認）。
- **2026-09-21** 複数メンバー選択、新着トップ、無限スクロール（のち廃止）。
- **2026-09-19** 初期実装・初回デプロイ。
