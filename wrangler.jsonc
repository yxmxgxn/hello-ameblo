{
  "name": "hello-ameblo",
  "main": "src/worker.js",
  "compatibility_date": "2026-09-01",
  "assets": {
    "directory": "./public",
    "binding": "ASSETS",
    // すべて Worker を通す。合言葉(SITE_PASS)を画面にも掛けるため。
    // 合言葉をやめるときは ["/api/*"] に戻すと、静的ファイルは Worker を通らず配信される
    "run_worker_first": true
  },
  // 毎時7分に GitHub Actions のクロールを起動する(src/worker.js の scheduled)
  "triggers": {
    "crons": ["7 * * * *"]
  },
  // 1分あたりの同一IPからのAPI呼び出し上限(Workers のレート制限バインディング)
  "unsafe": {
    "bindings": [
      { "name": "RL", "type": "ratelimit", "namespace_id": "1001", "simple": { "limit": 120, "period": 60 } }
    ]
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "hello-ameblo",
      // ダッシュボード → ストレージとデータベース → D1 → hello-ameblo (APAC)
      "database_id": "92c4802b-5049-46bb-b464-a9b51fb7138b"
    }
  ]
}
